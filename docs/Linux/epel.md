```
『epel download』
例：epel-release-6-8.noarch.rpm 

リンクのアドレスをコピー。

wget https://dl.fedoraproject.org/pub/epel/epel-release-latest-7.noarch.rpm

sudo rpm -Uvh epel-release-latest-7.noarch.rpm
sudo yum -y install ansible
ansible --version

```