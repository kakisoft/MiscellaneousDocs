```
rails new

vi Gemfile
 -> therubyracer を有効化して（rails server を動かしたときにエラーが出ない）

rails s
 -> 192.168.78.90:3000


rails g scaffold Memo title:string body:text

rake db:migrate

rails s
 -> 192.168.78.90:3000/memos
```


```
git add .
git commit -m "initial commit"
```
